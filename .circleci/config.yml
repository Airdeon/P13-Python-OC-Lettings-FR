version: 2
jobs:
  build:
    docker:
      - image: cimg/python:3.11.1-alpine3.17
      - auth:
        username: airdeon
        password: dckr_pat_TG3oT0PEuKZqA4W9zOMmLaO36sI
    steps:
      - checkout
      - restore_cache:
          key: deps1-{{ .Branch }}-{{ checksum "requirements.txt" }}
      - run:
          command: |
            python3 -m venv venv
            . venv/bin/activate
            pip install -r requirements.txt
      - save_cache:
          key: deps1-{{ .Branch }}-{{ checksum "requirements.txt" }}
          paths:
            - "venv"
      - run:
          name: Running tests
          command: |
            . venv/bin/activate
            pytest
      - store_artifacts:
          path: test-reports/
          destination: python_app